# 暗影象棋 · Shadow Chess

- **仓库**: github.com/vv5200000/shadow-chess
- **定位**: 原创规则象棋变体（首步伪装 + 双向迷雾），纯 HTML/CSS/JS 零依赖
- **当前形态**: 网页版，含 **双人对战 / 单机 AI（可执白/执黑）/ 在线联机** 三种模式；计时 / 悔棋 / 代数记谱 / 三次重复判和
- **联机版**: `node server/server.js`（零依赖 SSE+POST 权威服务器，http://localhost:8787），建房/加入/断线自动重连/快照裁剪反作弊/双方超时判负
- **核心代码**:
  - `js/chess.js` (~350行): `ChessGame` 纯逻辑类，零 DOM 零依赖 → Node/浏览器双端复用（含三次重复判和）
  - `js/ai.js` (170行): `ShadowAI` 公平博弈 AI（negamax + alpha-beta，depth 3）
  - `js/board.js` (~210行): DOM 渲染层，click 交互；联机模式走 `getMovesAsync` 钩子向服务器要走法（客户端无真实棋盘）
  - `js/net.js` (~160行): 联机客户端（EventSource 自动重连 + fetch POST，token 持久化 localStorage）
  - `js/app.js` (~450行) / `css/style.css` (~840行): UI 逻辑 + 样式（1024/600 响应式断点）
  - `server/server.js` (~380行): 零依赖联机服务器——房间码 + 服务端权威 + 视角裁剪快照 + SSE/POST + 静态服务
  - `tests/ai.test.js`: AI + 规则测试（22 断言，`node tests/ai.test.js`）
  - `tests/net.test.js`: 联机端到端 40 断言（双无头代理完整对局 / 快照裁剪 / 超时判负 / 断线重连 / 记谱）

---

## 决策记录

### 2026-09-04（会话 5）—— 联机版 P0 ✅ 已交付
1. **架构落地（延续会话 1 决策）**: 服务端权威——服务器持真实棋盘，客户端只收视角裁剪快照（反作弊核心）；chess.js 导出改造后 Node 直接复用（AI 22 测 + 联机服务器同一份引擎）
2. **协议**: 下行 SSE `/api/events`（snapshot 含 board/turn/timers/history/lastMove/oppOnline；tick 计时权威；opp 在线广播）+ 上行 POST `/api`（create/join/moves/move/promote/leave）；token 由服务器签发、localStorage 持久化；回合约减半不适用（联机无）
3. **快照裁剪**: 每个 cell 只发 `{own,color,revealed,type?,pos?}`——己方未揭示带 pos（渲染按位置规则提示），对方一律不泄露 type；终局 reveal 全部
4. **断线重连**: EventSource 自动重连 + 每次重连服务器首推权威快照校准；token 会话保留座位（对手看到"离线"），超时判负
5. **超时判负**: 服务器权威计时器（tick 广播），CTL 3.5× 容差防竞态；测试覆盖（2s/局 + 5 连败计数）
6. **客户端 UI**: 建房即进棋盘（顶部房间码 + 对手在线标签）；异步走法竞态修复（board.js `_movesPending`/`_pendingTarget`——快速点击丢选择的真 bug）；AUTH_FAILED 静默回大厅（不用 alert，避免阻塞页面）
7. **验收**: 40/40 联机测试 + 浏览器真实双端对局（建房→黑方协议客户端加入→白走 a3→黑应→快照流转、视角裁剪、NOT_YOUR_TURN 锁定）+ reload 会话恢复
8. **已知边界（P0 不修）**: leave 释放座位但 join 仅坐黑席（断线丢 session 后需等房间 GC）；房间 GC 空房 10min 清理

### 2026-09-02（会话 2）—— AI 对手先行 ✅ 已交付
1. **开工顺序拍板**: 单机 AI 先行（取代联机先行；用户指令两次反转，以最新为准——联机仍是后续主线）
2. **AI 方案落地**: `ShadowAI` —— **公平博弈**视角（只用公开信息，不偷看隐藏身份）
   - 走法生成: 100% 复用 `ChessGame.getLegalMoves`（未揭示按所在格位置规则 + 王安全过滤），与人类玩家同一套规则
   - 搜索: negamax + alpha-beta，默认 depth 3（node 实测 10-20ms/步；浏览器首手约 1s）
   - 估值: 已揭示按真实子力价值；未揭示按"剩余棋子池期望价值"；将军罚分 30；每枚未揭示己方子 +4（信息保密价值）；残局（≤14 子）王逼近 +0.6×(14−王距) 打破死局僵持
   - 升变: 搜索与实战一律自动变后（后续可加 N 升变特判）
3. **引擎新增规则**: **三次局面重复判和**（`gameState='draw'`，指纹含棋子身份/揭示状态/行棋方；undo 正确回退计数）—— 原引擎无任何和棋规则，AI 对弈会无限搅局暴露此缺口；对双人模式同样适用
4. **UI**: 启动即模式弹窗（⚔️双人 / 🤖AI执白 / 🤖AI执黑）；AI 自动回合（600ms 思考缓冲 + "AI 思考中"徽章，令牌防新局/悔棋竞态）；AI 模式无 pass 遮罩、悔棋一次撤销"自己最近一手 + AI 应手"；AI 执黑时棋盘翻转视角
5. **测试 22/22**: 一步吃王 / 被将军必逃 / 白送大子必吃 / 升变走法 / AI 自对弈 6 局全合法且终局（平均 69 回合）/ depth-3 性能 / 三次重复判和 + 悔棋计数回落
6. **浏览器冒烟通过**: 模式选择 → 人类落子 → AI 自动应招 → 悔棋 → 双人回归 → AI 执黑先手

### 2026-09-02（会话 1）
1. **定名**: 暗影象棋（Shadow Chess）
2. **手机版路径**: 网页响应式 + PWA（tap 天然等于 click，逻辑零改动）；不原生重写
3. **联机架构**: **服务端权威**（暗棋迷雾决定——不能把完整棋盘发给客户端，否则 DevTools 一看全作弊）
   - 服务器持有真实棋盘，只给每玩家发"其视角裁剪快照"（未揭示的己方/对方都渲染为 ?）
   - 棋规逻辑 100% 复用 chess.js（Node 直接跑），改导出即可
   - 复用弯道猛兽 src/net 已验证的架构经验（权威服务器 + token 重连）
4. **微信小程序**: 技术上可行但**正式上线被资质卡死**（游戏/棋牌类目需企业主体，内购需版号）→ 结论：**不做正式上线**，最多体验版供朋友测试
5. **变现路线**: **产品变现**
   - 路径: 单机 AI ✅ → 联机版（P0 ✅）→ 英文版 + itch.io（$3-5）→ 数据 OK 再 CrazyGames 分成 / Steam 海外区
   - 预期: 年收入几千美元级已算成功；真实价值 = 跑通"代码→商品"闭环
   - 护城河 = 规则原创性 + 完成度

---

## 进展追踪

| 日期 | 里程碑 | 测试 | 提交 |
|---|---|---|---|
| 2026-09-02 | 定名 + 架构决策（服务端权威） | — | — |
| 2026-09-02 | AI 对手（negamax depth-3 + 三次重复判和） | ai 22/22 | — |
| 2026-09-04 | **联机版 P0**：权威服务器 + 视角裁剪 + 断线重连 + 超时判负 | ai 22 + net 40 | `ed17668` |
| 进行中 | P1：联机悔棋 / 部署公网 | — | — |

联机 P0 验收（2026-09-04 真实浏览器双端）：建房→黑方加入→白 a3→黑 a6→快照流转/记谱/锁定/视角裁剪正确；reload 会话恢复；服务器 40 测 + AI 22 测全绿。

## 待办（下次继续）

- [ ] **联机 P1**: 联机悔棋（请求 → 对方同意）、渲染优化（对局中隐藏房间操作 UI）
- [ ] **部署**: 服务器已零依赖单文件，上传轻量 VPS（阿里云/腾讯云 Node 单进程）即可公网对战
- [ ] **联机健壮性**: 房间超时回收（leave 座位复用）、对局结束房间清理
- [ ] AI 打磨（可选）: 难度分档（depth 2/3/4）、N 升变、计时弱化（AI 首手在低端手机上 ~1s 可接受）

## 联机版已实施技术要点（会话 1 评估 → 现已落地）

- ✅ 计时器在服务器（权威 tick 广播，客户端仅展示）
- ⏳ 悔棋改"请求 → 对方同意"（P1）
- ✅ 断线重连（EventSource 自动重连 + 首推快照校准）
- ✅ 协议: 上行 POST `{a,room,token,...}` + 下行 SSE snapshot 广播；服务端校验 makeMove + reveal + 裁剪
- ✅ 部署形态: Node 单进程静态服务 + API（回合制并发压力极小）

## 已知边界（记录不修）

- 走子吃王、且吃王格恰为己方升变格（如黑王把自己挪到 a1 又被白兵吃掉）→ 走 makeMove 升变分支，不判 `won`，王消失后游戏继续。现实中几乎不可达（对方王要主动到你的底线且被你兵吃），先记着。
- 联机 leave 释放座位但 join 仅坐黑席：玩家断线丢 localStorage 会话后无法回原房间（需等房间 GC）；P1 修（join 认领空置座位 + 等待窗口）