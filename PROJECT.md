# 暗影象棋 · Shadow Chess

- **仓库**: github.com/vv5200000/shadow-chess
- **定位**: 原创规则象棋变体（首步伪装 + 双向迷雾），纯 HTML/CSS/JS 零依赖，~1700 行
- **当前形态**: 本地双人同屏网页版（pass-screen 防偷看），含计时 / 悔棋 / 代数记谱
- **核心代码**（已通读）:
  - `js/chess.js` (320行): `ChessGame` 纯逻辑类，零 DOM 零依赖 → **可双端复用**
  - `js/board.js` (193行): DOM 渲染层，click 交互 → **需重写为视图模型**
  - `js/app.js` (232行) / `css/style.css` (751行): UI 逻辑 + 样式（已有 1024/600 响应式断点）

---

## 决策记录

### 2026-09-02（会话 1）
1. **定名**: 暗影象棋（Shadow Chess）
2. **手机版路径**: 网页响应式 + PWA（tap 天然等于 click，逻辑零改动）；不原生重写
3. **联机架构**: **服务端权威**（暗棋迷雾决定——不能把完整棋盘发给客户端，否则 DevTools 一看全作弊）
   - 服务器持有真实棋盘，只给每玩家发"其视角裁剪快照"（未揭示的己方/对方都渲染为 ?）
   - 棋规逻辑 100% 复用 chess.js（Node 直接跑），改导出即可
   - 复用弯道猛兽 src/net 已验证的架构经验（权威服务器 + token 重连）
4. **微信小程序**: 技术上可行（3-5 天原型，云开发可免运维，好友分享卡片 + openid 免登录 + 订阅消息推送是最大优势，可模仿天天象棋的模式而非其 canvas 技术形态）；但**正式上线被资质卡死**（游戏/棋牌类目需企业主体，内购需版号）→ 结论：**不做正式上线**，最多体验版供朋友测试
5. **变现路线**: **产品变现**（用户明确：开发过程 AI 辅助，不做内容变现/教程）
   - 路径: 联机版 → 单机 AI 对手 → 英文版 + itch.io（$3-5）→ 数据 OK 再 CrazyGames 分成 / Steam 海外区（无需国内版号）
   - 双方签订预期: 年收入几千美元级已算成功；真实价值 = 跑通"代码→商品"闭环
   - 护城河 = 规则原创性 + 完成度（AI 已拉平代码工艺门槛）

---

## 待定事项（下次继续）

- [ ] **开工顺序二选一**（用户未定）:
  - A. 联机版先行（复用已验证 net 架构，价值最大）
  - B. AI 对手先行（1-2 天出"能玩"里程碑，单机可试玩，用全知视角 + minimax + 同一套走法生成）
- [ ] 技术验证第一步（P0）: chess.js 双端导出改造 + 最小 Node 服务端（房间码 + 双方视角互通跑通一局）

## 联机版已知技术要点（已评估，实施时对照）

- 计时器必须上服务器（现为客户端 setInterval）
- 悔棋改为"请求 → 对方同意"
- 断线重连是必需品（手机锁屏必断，非可选项）
- 协议: `{type:'move', from, to}` + 视角快照广播；服务端校验 makeMove 与 reveal
- 部署: 国内访问用阿里云/腾讯云轻量 Node 单进程即可（回合制，并发压力极小）